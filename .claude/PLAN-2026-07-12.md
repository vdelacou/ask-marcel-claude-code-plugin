# Plan - all next steps (written 2026-07-12)

Forward-looking companion to the audit board (`.claude/AUDIT-2026-07-11.md`, which records
what already happened). Each item: owner, definition of done (DoD), dependencies. Work top
to bottom; anything can be picked up by a fresh session with zero conversation context -
this file plus CLAUDE.md plus the referenced files are the full briefing.

Repo state when written: main at `docs(upstream): PR brief for create-forward-draft`,
7 commits ahead of origin, 427 tests green, all gates live (hooksPath configured).

---

## Phase A - unblock and prove (user-led, ~1 hour total)

**A1. Push.** Owner: user says push, session runs it. DoD: `git status -sb` shows main
in sync with origin; no force needed.

**A2. Sign in.** Owner: USER (interactive browser, cannot be done by a session).
`bun scripts/login.ts` from the repo root with a 10-minute timeout; `--fresh` only if it
loops or re-prompts. Context: the refresh token is confirmed dead server-side (access
token expired 2026-07-11 12:01, refresh refused; the 20s command deadline is what keeps
this from popping browsers mid-command). DoD: `bun scripts/doctor.ts` shows `auth: ok`
and answers in under ~3s.

**A3. LESSONS append (needs the user's yes - append-only journal).** Two candidates:
(a) core.hooksPath is per-clone opt-in config, so gates silently do not run on a fresh
clone until it is set - verify it before trusting any "gates passed";
(b) piped gate output lies twice: `cmd | grep/tail` makes && read the filter's exit, and
`$?` after a pipeline reads the last stage - always read a gate's verdict from its own
unpiped exit code. DoD: entries appended + committed, or explicitly declined.

**A4. First live inbox-zero run.** Owner: user at the gates, session drives. Launch
Claude Code from the folder where `data/` lives (or set ASK_MARCEL_HOME), say
"inbox zero". Exercise deliberately: the run-phase ladder to `wrapped`, verdict parsing
(scout bundle -> parse-verdicts), package validation (validate-package retry path if it
fires), consuming per-email drains, one deliberate DEFER at Gate 1 ("thread X until
<date>"), a scheduling email through calendar-view if one exists, watermark advance, the
run report + drift line. DoD: one run reaches `wrapped`; `data/reports/<run-id>.md`
exists; friction notes recorded on the audit board; any bug becomes a board item.

**A5. Post-run capture.** Owner: session. Record 3-5 real threads (anonymized ids fine,
content stays local) as eval fixtures for D1; run the user's /reflect to process the
corrections queue. DoD: fixtures saved under data/scratch or a local eval dir (never
committed if they contain real content); /reflect queue empty.

---

## Phase B - forward-draft (redirect stance becomes real)

**B1. Upstream command.** DONE 2026-07-12 (user shipped ask-marcel-office-cli 2.1.0).

**B1-orig.** Owner: a session opened in
`~/Documents/CODE/ask-marcel/ask-marcel-office-cli`, following
`.claude/UPSTREAM-create-forward-draft.md` (in THIS repo - self-contained brief; mirrors
create-reply-draft two-step + guard + tests + registration). That repo's CLAUDE.md and
gates govern. DoD: command + symmetric tests green there, registered in index +
graph-scopes, CHANGELOG, minor version bump committed.

**B2. Publish.** DONE 2026-07-12: 2.1.0 published by the user.

**B3. Plugin slice.** DONE 2026-07-12: forward-apply use-case + script (mirrors draft-apply gate, 95.8% mutation), redirect stance wired in inbox-zero + researcher, README/SPEC/board updated, dep bumped to ^2.1.0. Original scope below.

**B3-orig.** Owner: session. `bun update
ask-marcel-office-cli`; new `forward-apply` path behind the SAME user_approved state gate
as draft-apply (either a sibling script or a `--forward-to` mode - decide by whichever
keeps draft-apply under the 300-line commit cap); researcher's redirect stance wired to
it in inbox-zero Phase 4; tests + mutation >= 90 on touched files; README/SPEC lines.
DoD: an approved redirect produces an UNSENT forward draft with the comment above the
quote and the chosen recipients; never sends; board updated.

---

## Phase C - activation (after A4 proves the flow)

**C1. Schedules.** (Setup text names the exact --mode/--scope commands as of 2026-07-12; live registration stays user-side.) Setup skill step 10: weekday pre-research (time the user picks, scan
`--mode pre-research --scope since-watermark`) + weekly kb-gardener. DoD: both scheduled
tasks registered; the first overnight run leaves researched packages + a report, and the
morning interactive session resumes it (`state.ts resume`) straight into Phase 4.

**C2. Profile hygiene.** Seed `data/profile/never-capture.txt` (setup step 6) and
refresh user.md if stale. DoD: files exist; a test candidate naming a listed term is
refused at append (verify once, live).

**C3. First gardener run.** On the live KB: full lint (now incl. broken links, stale,
orphans), index regen, Phase 2 plan reviewed interactively. DoD: lint report clean or
each finding either fixed or accepted on the board.

---

## Phase D - second quality wave (build sessions, order by value)

**D1. Tier-2 evals (SPEC §10, still deferred).** Turn A5's recorded threads into eval
fixtures: expected triage verdicts and package-shape properties, runnable via the
skill-creator eval harness. DoD: baseline scores recorded; a prompt edit that regresses
triage or package quality fails an eval before it ships.

**D2. kb-ask skill.** DONE 2026-07-12: skills/kb-ask/SKILL.md (search module standalone, read-only, cited). Standalone "ask my KB" (search module + main-thread qmd query
escalation, read-only, cites like the researcher). DoD: skill answers a KB question with
citations without entering the inbox flow.

**D3. Resume affordance.** DONE 2026-07-12: state.ts list-runs + inbox-zero step 1b. `state.ts list-runs` (scan data/scratch for non-wrapped runs)
+ an inbox-zero step-0.5 that offers to resume instead of re-scanning. DoD: a run
interrupted mid-Phase 4 is listed and resumable without touching state.json by hand.

**D4. KB backup.** DONE 2026-07-12 (setup step 7, local-only git init). Optional setup step: `git init` inside data/ (local-only, never a
remote) so KB/profile history survives mistakes and disk moves get easier. DoD: offered
at setup, documented; declining leaves everything as-is.

**D5. Embed economy.** DONE 2026-07-12: SPEC decision 24 - qmd update unconditional, embed best-effort; skills carry the tolerance. Wrap-up currently runs `qmd update && qmd embed` every run; embed
only serves the optional main-thread `qmd query` escalation. Decide: skip embed when the
GGUF models are absent, or keep unconditionally. DoD: decision recorded (SPEC decision
list) + implemented if changed.

---

## Phase E - platform and polish (later, opportunistic)

- **E1** Windows verification run (code is path-safe by construction, never executed there).
- **E2** Upstream additions worth their own briefs when needed: add-attachment-to-draft,
  Teams-chat context source for threads that continue in chat.
- **E3** Raise caps 25/4/2 -> 50/8/4 (decision 15) after several clean runs.
- **E4** Optional: GitHub support ticket to purge cached pre-rewrite objects server-side.
- **E5** Release v0.2.2 (or 0.3.0 if B3 lands) once C proves out; bump manifests + tag +
  /plugin update on installs.

---

## Standing risks / watch-list

- The 20s auth deadline turns a mid-run token death into fast failures with a login
  remedy - if A4 shows one, finish the run after login; state is resume-safe.
- Per-file mutation laggards riding in green aggregate commits (run-report 94.7, deferral
  94.3, waiting-on 95.5 are now fine; kb-lint 89.8 solo) - any future commit staging
  kb-lint.ts ALONE must harden it first.
- deferrals due --consume hands the thread to the CURRENT run; if that run dies before
  Gate 1, the deferral is consumed but unhandled - the follow-ups skill and scratch
  reports are the safety net; revisit if it bites.
